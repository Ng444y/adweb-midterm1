import { ReplaceAtSymbolPipe } from './replace-at-symbol.pipe';

describe('ReplaceAtSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceAtSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
